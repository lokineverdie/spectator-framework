<?xml version="1.0" encoding="UTF-8"?>
<memory-prompts>
  
  <!-- Basic Memory Reference Usage -->
  <memory-reference-pattern>
    <description>Simple pattern for agents to use memory references</description>
    <usage>
      When you need to store information:
      1. Create memory entry: createEntry(content, category, ttlSeconds)
      2. Share reference with other agents: {reference.id}
      3. Check if still valid: reference.isExpired()
    </usage>
    <example>
      // Store user preference
      let ref = createEntry(userPrefs, "working", 3600);
      // Share with other agent
      "Use memory reference: " + ref.id
    </example>
  </memory-reference-pattern>

  <!-- Memory Categories -->
  <memory-categories>
    <working-memory>
      <description>Short-term session data</description>
      <default-ttl>3600</default-ttl>
    </working-memory>
    <episodic-memory>
      <description>Conversation history</description>
      <default-ttl>86400</default-ttl>
    </episodic-memory>
    <semantic-memory>
      <description>Facts and knowledge</description>
      <default-ttl>604800</default-ttl>
    </semantic-memory>
  </memory-categories>

</memory-prompts>